import React from 'react'

const ModalList = (events) => {
  return (
    <div>
        {/* {events.map((event) => {
            <p>{event.title}</p>
        })} */}
    </div>
  )
}

export default ModalList